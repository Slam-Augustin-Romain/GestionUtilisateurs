{% extends 'base.html.twig' %}

{% block title %}Classe{% endblock %}

{% block body %}
    <h1>Classe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classe.id }}</td>
            </tr>
            <tr>
                <th>Nom_classe</th>
                <td>{{ classe.nomClasse }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('classe_index') }}">Retour à la liste</a>

    <a class="btn btn-primary" href="{{ path('classe_edit', {'id': classe.id}) }}">Modifier</a>

    {{ include('classe/_delete_form.html.twig') }}
{% endblock %}
