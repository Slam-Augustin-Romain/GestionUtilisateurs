{% extends 'base.html.twig' %}

{% block title %}Modification de Classe{% endblock %}

{% block body %}
    <h1>Modifier la classe</h1>

    {{ include('classe/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary" href="{{ path('classe_index') }}">Retour à la liste</a>

    {{ include('classe/_delete_form.html.twig') }}
{% endblock %}
